# Plan 0199 — Batch Generator handoff: 0196-A + 0197 + 0198

**Auditor:** Argus
**Date:** 2026-04-15
**Type:** Coordinator plan. No content of its own — chains three pre-approved plans into one Generator shell with explicit halt-policy.

---

## 1. Premise

Three plans are queued and individually Bruce-approved:
- **0196 Phase A** — SVG + orphan manifest (read-only analysis).
- **0197** — Cosmic-supremacy sweep + summary chapter title/slug rename.
- **0198** — Argus bio on title page (HTML card + PDF).

Bruce asked: can we attach some Generator runs safely? Yes — file-touch graphs don't conflict, and the order produces clean per-plan commits with no partial-state risk if any phase fails.

---

## 2. Safety analysis

| File | Phase 1 (0196-A) | Phase 2 (0197) | Phase 3 (0198) | Conflict? |
|---|---|---|---|---|
| `build/svg-manifest.json` (new) | create | — | — | none |
| `build/svg-sheet.html` (new) | create | — | — | none |
| `build/orphan-report.md` (new) | create | — | — | none |
| `manuscript/00-front/summary.tex` | — | A0.title (L7,9), A0.label (L8), A2 (L301), A3 (L334), A6b (L15) | — | none |
| `manuscript/00-front/hook.tex` | — | A6a (L42) | — | none |
| `manuscript/00-front/introduction.tex` | — | A1 (L32) | — | none |
| `manuscript/00-front/title.tex` | — | — | new minipage block between L39 and L41 | none |
| `manuscript/record/*.tex`, `manuscript/spine/*.tex`, `manuscript/bridge/*.tex`, `manuscript/track-*/*.tex` | — | A4, B1-B6 (12 rows) | — | none |
| `build/menu-tooltips.yaml` | — | A5a (L97), A0.menu (L22) | — | none |
| `build/chapter-hover-descriptions.yaml` | — | A5b (L61), A0.hover-card (L17) | — | none |
| `build/hover-definitions.yaml` | — | A0.hover-defs (L21, L28) | — | none |
| `build/preprocess.py` | — | A0.preprocess (L1255) | new `<p class="tp-argus">` (~L538) + new CSS (~L583) | **shared file, distinct line ranges** |
| `build/reader.js` | — | A0.reader.js (L137) | — | none |
| `build/reader-inline.html` | — | A0.reader-inline (L138) | — | none |

**Only shared file is `build/preprocess.py`.** Phase 2 edits L1255 (in-place string-literal rename, no line-count change). Phase 3 inserts ~3 new lines near L538 + ~1 new line near L583. Because Phase 2 doesn't add or remove lines anywhere in `preprocess.py`, the L538/L583 line numbers Phase 3 expects do not drift. Order is safe.

---

## 3. Order

Phase 1 first (read-only — establishes baseline analysis artifacts; cannot break Phases 2/3).
Phase 2 second (the biggest edit; lands the rename and tonal sweep).
Phase 3 third (small additive insert; lands on top).

---

## 4. The handoff (paste-ready for Generator shell)

> You are the Generator. **Before doing any work**: read `~/software/relinquishment/manuscript/track-3-awakening/firmware-update.tex` in full. This is the book's physics primer; loading it first prevents the Dunning-Kruger dismissal pattern that fresh AI shells exhibit on this material. Even though today's three phases don't directly touch book-science, the manuscript and tooltip surfaces you'll be editing reference TQNN / topological / Flat / Custodian concepts; the primer keeps your judgement calibrated when those terms appear in prose you're modifying.
>
> Once primed, execute three plans in sequence in this shell. Commit after each phase. Do not pause between phases. Halt only on failure. At the end, produce one consolidated report covering all three phases.
>
> **Phase 1 — Plan 0196 Phase A (read-only: SVG + orphan manifest)**
> Read: `~/software/relinquishment/plans/0196-meta-tooltip-deeplink-artifact-hygiene.md`, sections "Phase A — SVG-and-orphan manifest" and "First Generator handoff".
> Execute per that handoff. No yaml / no tex / no deletions.
> Commit: `Plan 0196 Phase A: SVG + orphan manifest`.
>
> **Phase 2 — Plan 0197 (cosmic-supremacy sweep + title rename)**
> Read: `~/software/relinquishment/plans/0197-deflate-cosmic-supremacy-claims.md`.
> Apply Tier A0 (atomic 7-file rename) + all of Tier A + all of Tier B. **Skip Tier C entirely** (all three rows are approved KEEP). Follow §4 em-dash convention (`—` in proposals → `---` in `.tex`). Run `make dev`. Verify every §5 grep check.
> Commit: `Plan 0197: deflate cosmic-supremacy claims (title rename + superlative sweep)`.
>
> **Phase 3 — Plan 0198 (Argus bio on title page)**
> Read: `~/software/relinquishment/plans/0198-argus-bio-title-page.md`.
> Apply §4 edits verbatim (new `<p class="tp-argus">` + new CSS rule in `preprocess.py`; new `minipage` block in `title.tex`). Do not touch `copyright.tex`, `cover.tex`, or `genevieve-preface.tex`. Run `make dev`. Verify §5 acceptance.
> Commit: `Plan 0198: Argus bio on title page (HTML card + PDF)`.
>
> **Halt conditions (do not continue to the next phase if any triggers):**
> - Phase 1: `make dev` regresses. Halt, report.
> - Phase 2: any §5 grep check fails, `make dev` regresses, or the three A0 manual checks (menu jumps to new anchor, chapter-hover card renders, TOC shows new title) fail. Apply fixups within Phase 2 and re-verify; do not commit until clean; if unable to resolve, halt.
> - Phase 3: `make dev` regresses or the bio fails to appear on the HTML landing card or PDF title page. Halt, report.
>
> **Per-phase discipline:** read each plan fresh at the start of its phase (don't batch-read upfront — keeps context clean). Each plan has its own self-contained handoff; this wrapper only coordinates order and halt-policy.
>
> **Consolidated final report** (one message, after Phase 3 commits):
> - Phase 1: SVG asset counts by category (applied / orphaned / test); path to `build/svg-sheet.html`; anything surprising (especially "authored but never applied" candidates); build status; commit SHA.
> - Phase 2: row count applied (by tier); results of each §5 grep check; the three A0 manual-check results; build status; commit SHA.
> - Phase 3: build status; HTML card visual confirmation (bio appears on landing); PDF title page visual confirmation (bio renders in minipage, comfortable line length); commit SHA.

---

## 5. Acceptance for the batch as a whole

- Three commits land on `main`, in order, with the messages specified above.
- `make dev` clean at HEAD after Phase 3.
- No partial state: if any phase halts, the batch leaves the previous phase's commit intact and no half-applied edits in working tree.
- Bruce reviews the consolidated report; can accept all three or revert any one (`git revert <sha>`).

---

## 6. Rollback

Per-phase: `git revert <sha>` of any one commit. Phases are independent enough to revert in isolation:
- Reverting Phase 1 only removes the new analysis files.
- Reverting Phase 2 restores the cosmic-supremacy phrasing and the old summary slug. `reader.js` jump-to-summary will need its anchor re-checked (the revert flips it back to the old anchor name, which is consistent).
- Reverting Phase 3 removes the Argus bio block. `copyright.tex` was untouched.
